%label Sponsors

%ul
  - [{name: 'NetEngine', url: 'http://www.netengine.com.au'}].each do |sponsor|
    %li= link_to image_tag("sponsors/#{sponsor[:name].downcase.gsub(' ', '')}.png"), sponsor[:url], class: 'sponsor-link', title: sponsor[:name]
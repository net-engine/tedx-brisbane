%p= "Hi #{attendee.first_name},"

%p TEDxBrisbane #{Event.date.year} is happening soon. Click the button below to confirm your attendance at the event.

%p= email_button "Confirm", EmailLink.confirm(attendee)

= render 'emails/email_footer'
